Load a specialized skill by exact name from the `<available_skills>` index in the system prompt.

Use this tool when a listed skill name clearly matches the task. Do not invent a skill name.

The full skill instructions and bundled resources are loaded only when this tool is called.
