Resolve one tracked Chimera obligation.

Use this tool when the required review or update has been completed for a specific obligation.

Do NOT use it for skipped or irrelevant obligations — use `chimera_obligation_ignore` with a reason instead. Prefer `ref` copied from `Ref: obligation:<id>` output; `note` should briefly state the evidence or update that resolved it.

After this tool:
- Continue until all relevant active obligations are resolved or explicitly ignored.
